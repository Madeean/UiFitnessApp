<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/putih"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/iv_get_burn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:adjustViewBounds="true"
        android:src="@drawable/get_burn" />

    <TextView

        android:id="@+id/tv_get_burn"
        android:fontFamily="@font/poppins_boldd"
        android:textSize="24sp"
        android:textColor="@color/hitam"
        android:layout_marginTop="24dp"
        android:layout_marginHorizontal="30dp"
        android:layout_below="@id/iv_get_burn"
        android:text="Get Burn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:textAlignment="textStart"
        android:textSize="14sp"
        android:textColor="@color/coklatMuda"
        android:layout_marginHorizontal="30dp"
        android:layout_below="@id/tv_get_burn"
        android:text="Let’s keep burning, to achive yours goals, it \nhurts only temporarily, if you give up now \nyou will be in pain forever"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>



</RelativeLayout>